## 오늘의 주제: 
<img width="421" height="363" alt="image" src="https://github.com/user-attachments/assets/a746fcfc-bf2d-4a69-b9b5-c7dcd0adecbb" />

## 한 줄 핵심: (예: 클라이언트 사이드 번들 크기를 줄이고 성능을 최적화할 수 있다.)
js의 타입종류.
## 딱 하나만 기억한다면? 
- 래퍼 객체는 NULL,UNDEFINED에는 존재하지 않는다. 그외의 원시타입에 래퍼 객체가 존재한다.
- NUMBER.MAX_value, MAX_SAFE_INTEGER -> 내장 객체 number 의 정적 속성
```
리터럴은 JavaScript에서 값을 나타낸다. 변수가 아닌 문자 그대로 스크립트에 제공한 고정된 값이다.
```

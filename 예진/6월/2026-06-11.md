## 오늘의 주제: 정규식 표현식, symbol 맛보기
<img width="516" height="471" alt="image" src="https://github.com/user-attachments/assets/bd5539dd-a6cd-4935-9c82-584acddde928" />
정규표현식은 뭐라는지 모르겠다.
- `match`, `matchAll` : 문자열을 탐색할 때 사용.
                      각각 반환하는 값이 다름.
                      match: 배열
                      matchAll : 이터레이터 객체
## 한 줄 핵심: 정규표현식이란?
```
```
- 객체중 symbol 속성에 접근하는 방법
심볼은 고유한 식별자이므로 이름만으로는 접근할 수 없다.
일반적인 for...in 루프나 Object.keys() 메서드로는 조회되지 않는 것 확인.
```
Object.OwnPropertysSymbols()
Object.getOwnPropertySymbols()
```
## 딱 하나만 기억한다면? 
```
⭐ 정규표현식
⭐ symbol은 다른 타입과 다르다.
```
